# Coding challenges for Java.

Prerequisites:

* Installed maven
* Installed JDK

*. `git clone git@github.com:deadlydavid/java-challenges.git`
*. `mvn clean package`
*. `java -jar target/challenges-0.0.1-SNAPSHOT.jar`
